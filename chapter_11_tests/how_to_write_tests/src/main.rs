fn main() {
    // A test in Rust is a function that's annotated with test attribute.
    // Attributes in Rust are metadata about a piece of Rust code. (#[derive(Debug), #[test]]) on
    // the line before fn. 
    // To run test we use: "cargo test" command
    // The test module is included by default when creating a new project with cargo
}
